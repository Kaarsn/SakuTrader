{
  "name": "stock-ai-desktop",
  "version": "1.0.0",
  "private": true,
  "main": "main.js",
  "scripts": {
    "dev": "electron .",
    "prepare:runtime": "node scripts/prepare-runtime.js",
    "dist:win": "npm run prepare:runtime && electron-packager . \"IDX AI Analyst\" --platform=win32 --arch=x64 --out=dist --overwrite --ignore=\"^/runtime($|/)\" --extra-resource=./runtime && node scripts/postpack-optimize.js"
  },
  "dependencies": {
    "dotenv": "^16.4.7"
  },
  "devDependencies": {
    "electron": "^31.0.2",
    "electron-packager": "^17.1.2"
  }
}
